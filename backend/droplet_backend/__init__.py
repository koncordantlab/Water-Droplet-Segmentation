"""Water-droplet segmentation backend (phase-0 modularization of the
former Dash monolith). Numeric behavior is frozen by the golden masters
in tests/golden/expected/ — re-record via tests/golden/record_goldens.py only
when a numeric change is intended and reviewed."""
